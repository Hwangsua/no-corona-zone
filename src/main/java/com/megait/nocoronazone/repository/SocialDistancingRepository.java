package com.megait.nocoronazone.repository;

import com.megait.nocoronazone.domain.SocialDistancing;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

//public interface SocialDistancingRepository extends JpaRepository<SocialDistancing, String> {
//
//    @Query(value = "SELECT MAX(local_name) FROM SocialDistancing ")
//    Double maxDistancing();
//}
